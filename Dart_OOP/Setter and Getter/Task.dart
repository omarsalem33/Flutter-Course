// ============================================
// 01 — Encapsulation | Tasks
// ============================================
// Complete each task independently.
// ============================================

void main() {
  // Task 1:
  // Create a class `Phone` with private variables `_brand` and `_batteryLevel`.
  // Add a method `charge()` that increases battery by 10 (max 100).
  // Add a method `showStatus()` that prints brand and battery level.
  // YOUR CODE HERE:


  // Task 2:
  // Create a class `Student` with private variables `_name` and `_grade`.
  // The grade should only be between 0 and 100.
  // Add a method `updateGrade(int newGrade)` that only updates if valid.
  // Add a method `showInfo()` to display student info.
  // YOUR CODE HERE:


  // Task 3:
  // Create a class `TemperatureSensor` with a private variable `_celsius`.
  // Add a method `setTemperature(double temp)` that only accepts values
  // between -50 and 60.
  // Add a method `readTemperature()` that prints the current temperature.
  // YOUR CODE HERE:


  // Task 4:
  // Create a class `Password` with a private variable `_value`.
  // Add a method `changePassword(String oldPass, String newPass)` that
  // only changes the password if `oldPass` matches the current one.
  // Add a method `isCorrect(String attempt)` that returns true/false.
  // YOUR CODE HERE:

}