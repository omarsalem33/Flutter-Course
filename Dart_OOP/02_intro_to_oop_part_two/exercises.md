# 02 — Intro to OOP Part Two | Exercises

## Exercise 1: Name the Pillars
List the 4 pillars of OOP:
1. _______________
2. _______________
3. _______________
4. _______________

---

## Exercise 2: Match Pillar to Scenario
Match each scenario to the correct OOP pillar:

| Scenario | Pillar |
|----------|--------|
| A `Cat` and `Dog` class both inherit from `Animal` | ___ |
| A user can only access their balance through `getBalance()` | ___ |
| `draw()` works differently for `Circle` vs `Square` | ___ |
| A `Vehicle` abstract class defines `startEngine()` without implementation | ___ |

---

## Exercise 3: Real-World Encapsulation
Think of 3 real-world examples of encapsulation (hiding complexity behind a simple interface):

1. _______________
2. _______________
3. _______________

---

## Exercise 4: Inheritance Hierarchy
Draw a hierarchy for the following items. Which is the parent? Which are children?

Items: `Vehicle`, `Car`, `Truck`, `Motorcycle`, `ElectricCar`

```
        _______________
       /       |       \
  ________  ________  ________
     |
  ________
```

---

## Exercise 5: Polymorphism Examples
The method `calculateArea()` should work differently for each shape. Fill in what each would calculate:

| Shape | calculateArea() returns |
|-------|------------------------|
| Circle | _______________ |
| Rectangle | _______________ |
| Triangle | _______________ |

---

## Exercise 6: True or False
1. Encapsulation means making everything public. ( )
2. Inheritance allows code reuse between related classes. ( )
3. Polymorphism means "one form." ( )
4. Abstraction hides unnecessary details from the user. ( )
5. The 4 pillars are independent and never work together. ( )
