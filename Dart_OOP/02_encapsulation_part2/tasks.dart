// ============================================
// 02 — Encapsulation Part 2 | Tasks
// ============================================
// Complete each task independently.
// ============================================

void main() {
  // Task 1:
  // Create a class `UserAuth` with:
  // - A private variable `_password`
  // - A private method `_hashPassword(String pass)` that returns
  //   the password reversed (simple fake hash)
  // - A public method `register(String password)` that stores the hash
  // - A public method `login(String password)` that checks the hash
  // YOUR CODE HERE:


  // Task 2:
  // Create a class `ShoppingCart` with:
  // - A private list `_items` (List<String>)
  // - A private method `_isDuplicate(String item)` that checks if item exists
  // - A public method `addItem(String item)` that only adds if not duplicate
  // - A public method `showCart()` that prints all items
  // YOUR CODE HERE:


  // Task 3:
  // Create a class `FileManager` with:
  // - A private method `_hasValidExtension(String filename)` that checks
  //   if filename ends with .txt, .pdf, or .doc
  // - A private method `_generatePath(String filename)` that returns
  //   "/documents/" + filename
  // - A public method `saveFile(String filename)` that validates and saves
  // YOUR CODE HERE:


  // Task 4:
  // Create a class `GameCharacter` with:
  // - Private variables `_health`, `_armor`, `_name`
  // - A private method `_calculateDamage(int rawDamage)` that reduces
  //   damage based on armor (damage - armor, minimum 1)
  // - A public method `takeDamage(int rawDamage)` that applies calculated damage
  // - A public method `showStatus()` that prints name and health
  // YOUR CODE HERE:

}
