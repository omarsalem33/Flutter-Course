// ============================================
// 03 — Encapsulation Part 3 | Tasks
// ============================================
// Complete each task independently.
// ============================================

void main() {
  // Task 1:
  // Create a class `MusicPlaylist` with full encapsulation:
  // - Private: `_songs` (List<String>), `_currentIndex` (int)
  // - Private: `_isValidIndex(int index)`
  // - Public: addSong, removeSong, play, next, previous, showPlaylist
  // The play method prints "Now playing: [song name]"
  // YOUR CODE HERE:


  // Task 2:
  // Create a class `Inventory` with full encapsulation:
  // - Private: `_items` (Map<String, int>) — item name to quantity
  // - Private: `_isAvailable(String item)`
  // - Public: addItem(name, quantity), sellItem(name, quantity),
  //   checkStock(name), showInventory
  // sellItem should only work if enough stock exists
  // YOUR CODE HERE:


  // Task 3:
  // Create a class `QuizGame` with full encapsulation:
  // - Private: `_score`, `_questionsAnswered`, `_playerName`
  // - Private: `_calculatePercentage()`, `_getGrade()`
  //   (A: >=90, B: >=80, C: >=70, D: >=60, F: <60)
  // - Public: answerCorrectly, answerWrong, showResult
  // YOUR CODE HERE:


  // Task 4:
  // Create a class `ChatRoom` with full encapsulation:
  // - Private: `_messages` (List<String>), `_members` (List<String>),
  //   `_maxMembers` (int)
  // - Private: `_isMember(String name)`, `_isRoomFull()`
  // - Public: join(name), leave(name), sendMessage(name, message),
  //   showMessages
  // Only members can send messages, room has max capacity
  // YOUR CODE HERE:

}